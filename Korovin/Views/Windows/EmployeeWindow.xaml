<Window x:Class="Korovin.Views.Windows.EmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Korovin.Views.Windows"
        mc:Ignorable="d"
        Title="EmployeeWindow" Height="450" Width="450" WindowStartupLocation="CenterScreen">
    <StackPanel Margin="20" Name="EmployeeGrid">
        <TextBlock>
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0} {1} {2}">
                    <Binding Path="Lastname"/>
                    <Binding Path="Name"/>
                    <Binding Path="Surname"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>

        <Image Source="{Binding Photo}" Margin="10" Width="300"/>

        <TextBlock Text="{Binding Description}" Foreground="Black"/>
    </StackPanel>
</Window>
